#include<stdio.h>

    // argv is an array of pointers (pointer of pointers)
    // what is a string if not just an array of characters?
    //
int main(int argc, char *argv[]){
    
    for(int i=0;i<argc;i++){
        puts(argv[i]);
    }
    return 0;
};

void calls_main(){
char str1=

//finish late???
}
